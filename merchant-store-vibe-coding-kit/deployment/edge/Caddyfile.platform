{
  # Northflank/Railway terminate TLS and forward HTTP to this private service.
  auto_https off
  admin off
}

# Defines the (routes) snippet. Without this file import, `import routes` below
# fails config adaptation and the edge never starts.
import routes.caddy

:8080 {
  import routes
}
